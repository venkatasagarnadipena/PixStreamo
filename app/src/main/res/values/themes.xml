<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Mega_stream" parent="android:Theme.Material.NoActionBar">
        <!-- Optimized for TV: Prevent unnecessary resource loading -->
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
